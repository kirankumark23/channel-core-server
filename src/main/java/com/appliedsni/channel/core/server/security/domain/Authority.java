package com.appliedsni.channel.core.server.security.domain;

import com.appliedsni.channel.core.server.user.domain.UserEntity;

/**
 * Represents an authority granted to a {@link UserEntity}.
 *
 * @author gauri
 */
public enum Authority {
    ADMIN, USER
}